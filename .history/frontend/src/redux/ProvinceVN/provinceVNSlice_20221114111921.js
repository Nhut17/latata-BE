import { createSlice } from "@reduxjs/toolkit";


const initialState={

}

const provinceVNSlice = createSlice({
    name: 'province',
    initialState,
    extraReducers: {

    }
})

export default pro