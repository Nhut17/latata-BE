import { createSlice } from "@reduxjs/toolkit";


const initialState = {
    
}

const productReducer = createSlice({
    name: 'product',
    initialState,
    extraReducers: {

    }
})