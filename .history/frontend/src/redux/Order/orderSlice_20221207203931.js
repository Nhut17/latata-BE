import { createSlice } from "@reduxjs/toolkit"


const initialState = {

}





const orderSlice = createSlice({
    name: 'order',
    initialState,
    extraReducers: {

    }
})

export orderSlice.reducer